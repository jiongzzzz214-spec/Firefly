# build output
dist/

# generated types
.astro/

# dependencies
node_modules/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
astro-build.log

# environment variables
.env
.env.production

# macOS-specific files
.DS_Store

.vercel

package-lock.json
bun.lockb
yarn.lock

# ide
.idea
*.iml
.claude

# docs
Firefly-docs/
# 本地归档：博客相关资料（部署指南、辅助脚本、图标素材）。
# 只存在本机，不进版本控制 —— 避免 blog.sh 里的 `git add -A` 把它们扫进提交。
docs/

cache/
package/
.obsidian/

# AI 助手的工作区数据（项目记忆、进展日志）—— 本机专属，不进版本控制。
# 放在仓库根是为了「工作目录选 E:\blog 时自动加载项目记忆」，见 .workbuddy-ai/memory/MEMORY.md
.workbuddy-ai/