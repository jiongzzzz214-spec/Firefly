import type { SakuraConfig } from "../types/effectsConfig";

// 特效配置 - 集中管理所有动画特效

export const sakuraConfig: SakuraConfig = {
	// 是否启用樱花特效
	enable: true,

	// 是否允许用户在设置中切换
	switchable: true,

	// 樱花数量
	sakuraNum: 21,

	// 樱花越界限制次数，-1为无限循环
	limitTimes: -1,

	// 樱花尺寸
	size: {
		// 樱花最小尺寸倍数
		min: 0.5,
		// 樱花最大尺寸倍数
		max: 1.1,
	},

	// 樱花不透明度
	opacity: {
		// 樱花最小不透明度
		min: 0.3,
		// 樱花最大不透明度
		max: 0.9,
	},

	// 樱花移动速度
	//
	// ⚠️ 数值含义 = 「每秒位移」，不是「每帧位移」（2026-09-13 起）。
	// 动画已改为按时间推进（见 SakuraEffect.astro：位移 × k，k = 本帧耗时 ÷ 16.7ms），
	// 所以速度在所有刷新率下一致，改一次就全设备生效，不会再出现
	// 「Edge 上慢、iPad 上快」的偏差。
	//
	// 调整历史：
	//   原始模板值（当前）：水平 -1.7/-1.2、垂直 1.5/2.2、rotation 0.03、fadeSpeed 0.03
	//   2026-09-12  整体乘 0.53，降速版（水平 -0.9/-0.65、垂直 0.8/1.2、rotation/fadeSpeed 0.02）
	//   2026-09-13  改按时间推进后观感统一变慢，用户要求提速 → 恢复原始模板值
	//
	// 还想更快：把下面四个数整体乘 1.3（水平 -2.2/-1.55、垂直 1.95/2.85、rotation 0.04、fadeSpeed 0.04）。
	// 嫌太快：乘 0.7 回到约 2026-09-12 那档。
	speed: {
		// 水平移动
		horizontal: {
			// 水平移动速度最小值
			min: -1.7,
			// 水平移动速度最大值
			max: -1.2,
		},
		// 垂直移动
		vertical: {
			// 垂直移动速度最小值
			min: 1.5,
			// 垂直移动速度最大值
			max: 2.2,
		},
		// 旋转速度
		rotation: 0.03,
		// 消失速度，不应大于最小不透明度
		fadeSpeed: 0.03,
	},

	// 层级，确保樱花在合适的层级显示
	zIndex: 100,
};
